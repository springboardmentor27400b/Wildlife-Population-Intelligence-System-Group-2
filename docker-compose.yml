
services:
  postgres:
    image: postgres:15-alpine
    container_name: wpis-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Aarti
      POSTGRES_DB: wildlife_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d wildlife_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: wpis-backend
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=wildlife_db
      - DB_USER=postgres
      - DB_PASSWORD=${DB_PASSWORD}
      - SECRET_KEY=${SECRET_KEY}
      - ALGORITHM=HS256
      - ACCESS_TOKEN_EXPIRE_MINUTES=60
      - GOOGLE_CLIENT_ID=251145097240-g3h8t0l6ii219nhkv99mvgju6qo9c3ug.apps.googleusercontent.com
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GEMINI_MODEL=gemini-1.5-flash
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 15s
      timeout: 10s
      retries: 3
    volumes:
      - uploads:/app/uploads


  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_GOOGLE_CLIENT_ID=251145097240-g3h8t0l6ii219nhkv99mvgju6qo9c3ug.apps.googleusercontent.com
    container_name: wpis-frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  pgdata:
  uploads:
